# UCLA_UX_wireframe

Project for prototyping and wireframe for UCLA.

## Existing local project source

This repository is intended to become the remote for the existing local project at:

`\\wsl.localhost\Ubuntu\home\adamb\code\abrun-s\ucla\Recreate existing design layouts (Copy)`

## Initial import to this remote repository

From your local project directory, run:

```bash
git init
git branch -M main
git remote add origin <REMOTE_URL>
git add .
git commit -m "Initial import of existing UCLA UX wireframe project"
git push -u origin main
```
